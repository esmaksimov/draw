apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spacedeck-data
  namespace: draw
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path